services:
  - type: web
    name: cafesys-backend
    env: docker
    plan: free
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    startCommand: gunicorn cafesys.wsgi:application --bind 0.0.0.0:10000